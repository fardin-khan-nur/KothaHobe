## Packages
framer-motion | Smooth animations for UI transitions and video states
clsx | Utility for conditional class names
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
WebSocket connection at /ws
WebRTC requires STUN servers (using public Google STUN for demo)
Camera/Mic permissions required immediately on load
